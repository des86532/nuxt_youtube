export const state = () => ({
})

export const actions =  {
  nuxtServerInit ({ commit }, { req }) {
    // if (req.session.user) {
    //   commit('user', req.session.user)
    // }
    // console.log(req)
  }
}

export const mutations = {
}